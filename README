Install Eclipse IDE v:4.19.0
From Github, clone the project into your local machine and import the project into Eclipse for execution.
There is POM file which has maven repository dependencies of appium and TestNg which is required to run the project.
The program is designed using TestNg framework which has @BeforeClass,@AfterClass and @DataProvider annotatinos which helps to configure appium settings for different test methods.
Run Appium server from terminal in macbook manually.
Run the program as TestNg from eclipse. 

      
